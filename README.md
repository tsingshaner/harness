# harness

Small collection of agent-oriented **skills** (workflows and checklists) for tooling and release hygiene.

## Skills

| Skill | File | Summary |
|--------|------|---------|
| **changeset-add** | [`skills/changeset-add.md`](skills/changeset-add.md) | Use `pnpm changeset --empty` when interactive `pnpm changeset` fails (e.g. `ERR_USE_AFTER_CLOSE` in non-interactive shells), then fill front matter and notes from `git diff` and commit. |

Open a skill file for the full step-by-step workflow, validation checklist, and anti-patterns.
